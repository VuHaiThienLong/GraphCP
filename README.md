# GraphCP

# File Differences

*   **`Heuristics.ipynb`**: The baseline implementation using **NetworkX**. It calculates  local topological features like Adamic-Adar and Jaccard.

*   **`Embeddings.ipynb`**: The optimized implementation using **scikit-network**. It uses sparse matrices and **SVD Embeddings** for global feature extraction.